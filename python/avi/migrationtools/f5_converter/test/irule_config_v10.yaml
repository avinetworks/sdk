#iRule to be mapped to dummy datascript
- rule_name: http200
  type: VSDataScriptSet
#iRule to be mapped to dummy HTTPPolicy
- rule_name: testhttps
  type: HTTPPolicySet
#iRule to be mapped to given datascript
- rule_name: SNAT-to-XFF
  type: VSDataScriptSet
  avi_config: {'name': testabc, 'datascript':[{'evt': 'VS_DATASCRIPT_EVT_HTTP_REQ','script': 'if avi.http.get_query("intro", "true") == "hello%20world" then avi.http.add_header("intro", "true") end'}]}
#iRule to be mapped to given HTTPPolicy
- rule_name: F5-iRule-001
  type: HTTPPolicySet
  avi_config: { "http_request_policy": {
                "rules": [{"index": 1, "name": "rule-header-erase", "hdr_action": [{"action": "HTTP_REMOVE_HDR","hdr": {"name": "client-ip","value": {"val": null}}}]}]},"tenant_ref": "/api/tenant/?name=admin", "name": "Test-Profile-HTTP-Policy-Set", "is_internal_policy": false}
#iRule to be mapped to http to https redirect
- rule_name: _sys_https_redirect
  type: httptohttpsredirect
