# GENERATED FILE - DO NOT EDIT THIS FILE
---
swagger: '2.0'
info:
    version: '16.3'
    title: Avi API
    contact:
        name: Avi Networks Inc.
        url: https://avinetworks.com/contact-us
        email: support@avinetworks.com
    description: Pool API documentation
securityDefinitions:
    basicAuth:
        type: basic
        description: basic authentication
basePath: /api
paths:
    /pool:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/PoolApiResponse"
                401:
                    description: log in failed
        post:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: Pool object creation
                    required: true
                    schema:
                        $ref: "#/definitions/Pool"
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/Pool"
                401:
                    description: log in failed
    /pool/{uuid}:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: uuid
                    in: path
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/Pool"
                401:
                    description: log in failed
        put:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: Pool object creation
                    required: true
                    schema:
                        $ref: "#/definitions/Pool"
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/Pool"
                401:
                    description: log in failed
        patch:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: Pool object creation
                    required: true
                    schema:
                        $ref: "#/definitions/Pool"
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/Pool"
                401:
                    description: log in failed
        delete:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                204:
                    description: object deleted
                    schema:
                        type: string
                404:
                    description: not found

    /pool/{uuid}/scaleout:
        post:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: ServerScaleOutParams
                    required: true
                    schema:
                        $ref: "#/definitions/ServerScaleOutParams"
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        type: string
                401:
                    description: log in failed
    /pool/{uuid}/scalein:
        post:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: ServerScaleInParams
                    required: true
                    schema:
                        $ref: "#/definitions/ServerScaleInParams"
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        type: string
                401:
                    description: log in failed
definitions:
    AbPool:
        type: object
        required:
            -   pool_ref
        properties:
            pool_ref:
                description: "Pool configured as B pool for A/B testing object ref Pool."
                type: string
            ratio:
                description: "Ratio of traffic diverted to the B pool, for A/B testing"
                type: integer
                format: int32
    DiscoveredNetwork:
        type: object
        required:
            -   network_ref
        properties:
            network_ref:
                description: "Discovered network for this IP. object ref Network."
                type: string
            subnet:
                type: array
                items:
                    $ref: '#/definitions/IpAddrPrefix'
    FailAction:
        type: object
        required:
            -   type
        properties:
            backup_pool:
                $ref: '#/definitions/FailActionBackupPool'
            local_rsp:
                $ref: '#/definitions/FailActionHTTPLocalResponse'
            redirect:
                $ref: '#/definitions/FailActionHTTPRedirect'
            type:
                description: "Enables a response to client when pool experiences a failure. By default TCP connection is closed.Enum options - FAIL_ACTION_HTTP_REDIRECT, FAIL_ACTION_HTTP_LOCAL_RSP, FAIL_ACTION_CLOSE_CONN, FAIL_ACTION_BACKUP_POOL"
                type: number
                format: int64
    FailActionBackupPool:
        type: object
        required:
            -   backup_pool_ref
        properties:
            backup_pool_ref:
                description: "Specifies the UUID of the Pool acting as backup pool. object ref Pool."
                type: string
    FailActionHTTPLocalResponse:
        type: object
        properties:
            file:
                $ref: '#/definitions/HTTPLocalFile'
            status_code:
                description: "Enum options - FAIL_HTTP_STATUS_CODE_200, FAIL_HTTP_STATUS_CODE_503"
                type: number
                format: int64
    FailActionHTTPRedirect:
        type: object
        required:
            -   host
        properties:
            host:
                description: ""
                type: string
            path:
                description: ""
                type: string
            protocol:
                description: "Enum options - HTTP, HTTPS"
                type: number
                format: int64
            query:
                description: ""
                type: string
            status_code:
                description: "Enum options - HTTP_REDIRECT_STATUS_CODE_301, HTTP_REDIRECT_STATUS_CODE_302, HTTP_REDIRECT_STATUS_CODE_307"
                type: number
                format: int64
    HTTPLocalFile:
        type: object
        required:
            -   content_type
            -   file_content
        properties:
            content_type:
                description: "Mime-type of the content in the file."
                type: string
            file_content:
                description: "File content to used in the local HTTP response body."
                type: string
    HTTPRedirectAction:
        type: object
        required:
            -   protocol
        properties:
            host:
                $ref: '#/definitions/URIParam'
            keep_query:
                description: "Keep or drop the query of the incoming request URI in the redirected URI"
                type: boolean
            path:
                $ref: '#/definitions/URIParam'
            port:
                description: "Port to which redirect the request"
                type: integer
                format: int32
            protocol:
                description: "Protocol type. Enum options - HTTP, HTTPS"
                type: number
                format: int64
            status_code:
                description: "HTTP redirect status code. Enum options - HTTP_REDIRECT_STATUS_CODE_301, HTTP_REDIRECT_STATUS_CODE_302, HTTP_REDIRECT_STATUS_CODE_307"
                type: number
                format: int64
    HTTPReselectRespCode:
        type: object
        properties:
            codes:
                description: "HTTP response code to be matched."
                type: array
                items:
                    type: integer
                format: int32
            ranges:
                type: array
                items:
                    $ref: '#/definitions/HTTPStatusRange'
            resp_code_block:
                description: "Block of HTTP response codes to match for server reselect.Enum options - HTTP_RSP_4XX, HTTP_RSP_5XX"
                type: array
                items:
                    type: number
                format: int64
    HTTPServerReselect:
        type: object
        required:
            -   enabled
        properties:
            enabled:
                description: "Enable HTTP request reselect when server responds with specific response codes."
                type: boolean
            num_retries:
                description: "Number of times to retry an HTTP request when server responds with configured status codes."
                type: integer
                format: int32
            retry_nonidempotent:
                description: "Allow retry of non-idempotent HTTP requests."
                type: boolean
            svr_resp_code:
                $ref: '#/definitions/HTTPReselectRespCode'
    HTTPStatusRange:
        type: object
        required:
            -   begin
            -   end
        properties:
            begin:
                description: "Starting HTTP response status code"
                type: integer
                format: int32
            end:
                description: "Ending HTTP response status code"
                type: integer
                format: int32
    IpAddr:
        type: object
        required:
            -   addr
            -   type
        properties:
            addr:
                description: "IP address"
                type: string
            type:
                description: "Enum options - V4, DNS"
                type: number
                format: int64
    IpAddrPrefix:
        type: object
        required:
            -   ip_addr
            -   mask
        properties:
            ip_addr:
                $ref: '#/definitions/IpAddr'
            mask:
                description: ""
                type: integer
                format: int32
    NetworkFilter:
        type: object
        required:
            -   network_ref
        properties:
            network_ref:
                description: " object ref VIMgrNWRuntime."
                type: string
            server_filter:
                description: ""
                type: string
    PlacementNetwork:
        type: object
        required:
            -   network_ref
        properties:
            network_ref:
                description: " object ref Network."
                type: string
            subnet:
                $ref: '#/definitions/IpAddrPrefix'
    Pool:
        type: object
        required:
            -   name
        properties:
            a_pool:
                description: "Name of container cloud application that constitutes A pool in a A-B pool configuration, if different from VS app"
                type: string
            ab_pool:
                $ref: '#/definitions/AbPool'
            ab_priority:
                description: "Priority of this pool in a A-B pool pair. Internally used"
                type: integer
                format: int32
            apic_epg_name:
                description: "Synchronize Cisco APIC EPG members with pool servers"
                type: string
            application_persistence_profile_ref:
                description: "Persistence will ensure the same user sticks to the same server for a desired duration of time. object ref ApplicationPersistenceProfile."
                type: string
            autoscale_launch_config_ref:
                description: "Reference to the Launch Configuration Profile object ref AutoScaleLaunchConfig."
                type: string
            autoscale_networks:
                description: "Network Ids for the launch configuration"
                type: array
                items:
                    type: string
            autoscale_policy_ref:
                description: "Reference to Server Autoscale Policy object ref ServerAutoScalePolicy."
                type: string
            capacity_estimation:
                description: "Inline estimation of capacity of servers."
                type: boolean
            capacity_estimation_ttfb_thresh:
                description: "The maximum time-to-first-byte of a server."
                type: integer
                format: int32
            cloud_config_cksum:
                description: "Checksum of cloud configuration for Pool. Internally set by cloud connector"
                type: string
            cloud_ref:
                description: " object ref Cloud."
                readOnly: True
                type: string
            connection_ramp_duration:
                description: "Duration for which new connections will be gradually ramped up to a server recently brought online.  Useful for LB algorithms that are least connection based."
                type: integer
                format: int32
            created_by:
                description: "Creator name"
                type: string
            default_server_port:
                description: "Traffic sent to servers will use this destination server port unless overridden by the server's specific port attribute. The SSL checkbox enables Avi to server encryption."
                type: integer
                format: int32
            description:
                description: "A description of the pool."
                type: string
            domain_name:
                description: "Comma separated list of domain names which will be used to verify the common names or subject alternative names presented by server certificates if common name check (host_check_enabled) is enabled."
                type: array
                items:
                    type: string
            east_west:
                description: "Inherited config from VirtualService."
                type: boolean
            enabled:
                description: "Enable or disable the pool.  Disabling will terminate all open connections and pause health monitors."
                type: boolean
            fail_action:
                $ref: '#/definitions/FailAction'
            fewest_tasks_feedback_delay:
                description: "Periodicity of feedback for fewest tasks server selection algorithm."
                type: integer
                format: int32
            graceful_disable_timeout:
                description: "Used to gracefully disable a server. Virtual service waits for the specified time before terminating the existing connections  to the servers that are disabled."
                type: integer
                format: int32
            health_monitor_refs:
                description: "Verify server health by applying one or more health monitors.  Active monitors generate synthetic traffic from each Service Engine and mark a server up or down based on the response. The Passive monitor listens to client to server communication and raises or lowers the ratio of traffic destined to a server based on successful responses. object ref HealthMonitor."
                type: array
                items:
                    type: string
            host_check_enabled:
                description: "Enable common name check for server certificate. If enabled and no explicit domain name is specified, Avi will use the incoming host header to do the match."
                type: boolean
            inline_health_monitor:
                description: "The Passive monitor will monitor client to server connections and requests and adjust traffic load to servers based on successful responses.  This may alter the expected behavior of the LB method, such as Round Robin."
                type: boolean
            ipaddrgroup_ref:
                description: "Use list of servers from Ip Address Group object ref IpAddrGroup."
                type: string
            lb_algorithm:
                description: "The load balancing algorithm will pick a server within the pool's list of available servers.Enum options - LB_ALGORITHM_LEAST_CONNECTIONS, LB_ALGORITHM_ROUND_ROBIN, LB_ALGORITHM_FASTEST_RESPONSE, LB_ALGORITHM_CONSISTENT_HASH, LB_ALGORITHM_LEAST_LOAD, LB_ALGORITHM_FEWEST_SERVERS, LB_ALGORITHM_RANDOM, LB_ALGORITHM_FEWEST_TASKS"
                type: number
                format: int64
            lb_algorithm_consistent_hash_hdr:
                description: "HTTP header name to be used for the hash key."
                type: string
            lb_algorithm_hash:
                description: "Criteria used as a key for determining the hash between the client and  server.Enum options - LB_ALGORITHM_CONSISTENT_HASH_SOURCE_IP_ADDRESS, LB_ALGORITHM_CONSISTENT_HASH_SOURCE_IP_ADDRESS_AND_PORT, LB_ALGORITHM_CONSISTENT_HASH_URI, LB_ALGORITHM_CONSISTENT_HASH_CUSTOM_HEADER"
                type: number
                format: int64
            max_concurrent_connections_per_server:
                description: "The maximum number of concurrent connections allowed to each server within the pool."
                type: integer
                format: int32
            max_conn_rate_per_server:
                $ref: '#/definitions/RateProfile'
            name:
                description: "The name of the pool."
                type: string
            networks:
                type: array
                items:
                    $ref: '#/definitions/NetworkFilter'
            pki_profile_ref:
                description: "Avi will validate the SSL certificate present by a server against the selected PKI Profile. object ref PKIProfile."
                type: string
            placement_networks:
                type: array
                items:
                    $ref: '#/definitions/PlacementNetwork'
            prst_hdr_name:
                description: "Header name for custom header persistence"
                type: string
            request_queue_depth:
                description: "Minimum number of requests to be queued when pool is full."
                type: integer
                format: int32
            request_queue_enabled:
                description: "Enable request queue when pool is full"
                type: boolean
            rewrite_host_header_to_server_name:
                description: "Rewrite incoming Host Header to server name of the server to which the request is proxied.  Enabling this feature rewrites Host Header for requests to all servers in the pool."
                type: boolean
            rewrite_host_header_to_sni:
                description: "If SNI server name is specified, rewrite incoming host header to the SNI server name."
                type: boolean
            server_auto_scale:
                description: "Server AutoScale. Not used anymore."
                type: boolean
            server_count:
                description: ""
                type: integer
                format: int32
            server_name:
                description: "Fully qualified DNS hostname which will be used in the TLS SNI extension in server connections if SNI is enabled. If no value is specified, Avi will use the incoming host header instead."
                type: string
            server_reselect:
                $ref: '#/definitions/HTTPServerReselect'
            servers:
                type: array
                items:
                    $ref: '#/definitions/Server'
            sni_enabled:
                description: "Enable TLS SNI for server connections. If disabled, Avi will not send the SNI extension as part of the handshake."
                type: boolean
            ssl_key_and_certificate_ref:
                description: "Service Engines will present a client SSL certificate to the server. object ref SSLKeyAndCertificate."
                type: string
            ssl_profile_ref:
                description: "When enabled, Avi re-encrypts traffic to the backend servers. The specific SSL profile defines which ciphers and SSL versions will be supported. object ref SSLProfile."
                type: string
            tenant_ref:
                description: " object ref Tenant."
                type: string
            url:
                description: "url"
                readOnly: True
                type: string
            use_service_port:
                description: "Do not translate the client's destination port when sending the connection to the server.  The pool or servers specified service port will still be used for health monitoring.  This feature is only applicable for a Virtual Service configured with the application type set to L4."
                type: boolean
            uuid:
                description: "UUID of the pool"
                type: string
            vrf_ref:
                description: "Virtual Routing Context that the pool is bound to. This is used to provide the isolation of the set of networks the pool is attached to. The pool inherits the Virtual Routing Conext of the Virtual Service, and this field is used only internally, and is set by pb-transform. object ref VrfContext."
                readOnly: True
                type: string
    PoolApiResponse:
        type: object
        required:
            - count
            - results
        properties:
            count:
                type: integer
                format: int32
            results:
                type: array
                items:
                    $ref: "#/definitions/Pool"
    RateLimiterAction:
        type: object
        properties:
            file:
                $ref: '#/definitions/HTTPLocalFile'
            redirect:
                $ref: '#/definitions/HTTPRedirectAction'
            status_code:
                description: "HTTP status code for Local Response rate limit action.Enum options - HTTP_LOCAL_RESPONSE_STATUS_CODE_200, HTTP_LOCAL_RESPONSE_STATUS_CODE_403, HTTP_LOCAL_RESPONSE_STATUS_CODE_404, HTTP_LOCAL_RESPONSE_STATUS_CODE_429"
                type: number
                format: int64
            type:
                description: "Type of action to be enforced upon hitting the rate limit.Enum options - RL_ACTION_NONE, RL_ACTION_DROP_CONN, RL_ACTION_RESET_CONN, RL_ACTION_CLOSE_CONN, RL_ACTION_LOCAL_RSP, RL_ACTION_REDIRECT"
                type: number
                format: int64
    RateProfile:
        type: object
        properties:
            action:
                $ref: '#/definitions/RateLimiterAction'
            burst_sz:
                description: "Maximum number of connections or requests or packets to be let through instantaneously"
                type: integer
                format: int32
            count:
                description: "Maximum number of connections or requests or packets"
                type: integer
                format: int32
            explicit_tracking:
                description: "Explicitly tracks an attacker across rate periods"
                type: boolean
            fine_grain:
                description: "Enable fine granularity"
                type: boolean
            period:
                description: "Time value in seconds to enforce rate count"
                type: integer
                format: int32
    Server:
        type: object
        required:
            -   ip
        properties:
            availability_zone:
                description: "Availability-zone of the server VM."
                type: string
            description:
                description: "A description of the Server."
                type: string
            discovered_network_ref:
                description: "(internal-use) Discovered network for this server. This field is deprecated. object ref Network."
                type: array
                items:
                    type: string
            discovered_networks:
                type: array
                items:
                    $ref: '#/definitions/DiscoveredNetwork'
            discovered_subnet:
                type: array
                items:
                    $ref: '#/definitions/IpAddrPrefix'
            enabled:
                description: "Enable, Disable or Graceful Disable determine if new or existing connections to the server are allowed."
                type: boolean
            external_orchestration_id:
                description: "UID of server in external orchestration systems"
                type: string
            external_uuid:
                description: "UUID identifying VM in OpenStack and other external compute"
                type: string
            hostname:
                description: "DNS resolvable name of the server.  May be used in place of the IP address."
                type: string
            ip:
                $ref: '#/definitions/IpAddr'
            mac_address:
                description: "MAC address of server."
                type: string
            nw_ref:
                description: "(internal-use) This field is used internally by Avi, not editable by the user. object ref VIMgrNWRuntime."
                type: string
            port:
                description: "Optionally specify the servers port number.  This will override the pool's default server port attribute."
                type: integer
                format: int32
            prst_hdr_val:
                description: "Header value for custom header persistence."
                type: string
            ratio:
                description: "Ratio of selecting eligible servers in the pool"
                type: integer
                format: int32
            resolve_server_by_dns:
                description: "Auto resolve server's IP using DNS name"
                type: boolean
            rewrite_host_header:
                description: "Rewrite incoming Host Header to server name."
                type: boolean
            server_node:
                description: "Hostname of the node where the server VM or container resides"
                type: string
            static:
                description: "If statically learned."
                type: boolean
            verify_network:
                description: "Verify server belongs to a discovered network or reachable via a discovered network. Verify reachable network isn't the OpenStack management network"
                type: boolean
            vm_ref:
                description: "(internal-use) This field is used internally by Avi, not editable by the user. object ref VIMgrVMRuntime."
                type: string
    ServerId:
        type: object
        required:
            -   ip
            -   port
        properties:
            external_uuid:
                description: "This is the Pool servers external uuid"
                type: string
            ip:
                $ref: '#/definitions/IpAddr'
            port:
                description: ""
                type: integer
                format: int32
    ServerScaleInParams:
        type: object
        properties:
            reason:
                description: "Reason for manual scalein."
                type: string
            servers:
                type: array
                items:
                    $ref: '#/definitions/ServerId'
    ServerScaleOutParams:
        type: object
        properties:
            reason:
                description: "Reason for manual scaleout."
                type: string
    URIParam:
        type: object
        required:
            -   type
        properties:
            tokens:
                type: array
                items:
                    $ref: '#/definitions/URIParamToken'
            type:
                description: "URI param type. Enum options - URI_PARAM_TYPE_TOKENIZED"
                type: number
                format: int64
    URIParamToken:
        type: object
        required:
            -   type
        properties:
            end_index:
                description: "Index of the ending token in the incoming URI"
                type: integer
                format: int32
            start_index:
                description: "Index of the starting token in the incoming URI"
                type: integer
                format: int32
            str_value:
                description: "Constant string to use as a token"
                type: string
            type:
                description: "Token type for constructing the URI. Enum options - URI_TOKEN_TYPE_HOST, URI_TOKEN_TYPE_PATH, URI_TOKEN_TYPE_STRING, URI_TOKEN_TYPE_STRING_GROUP, URI_TOKEN_TYPE_REGEX"
                type: number
                format: int64
