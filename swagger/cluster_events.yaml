# GENERATED FILE - DO NOT EDIT THIS FILE
---
swagger: '2.0'
info:
    version: '16.3'
    title: Avi API
schemes:
    -   https
securityDefinitions:
    basicAuth:
        type: basic
        description: basic authentication
basePath: /api
paths:
definitions:
    ClusterLeaderFailoverEvent:
        type: object
        properties:
            leader_node:
                $ref: '#/definitions/ClusterNode'
            previous_leader_node:
                $ref: '#/definitions/ClusterNode'
    ClusterNode:
        type: object
        required:
            -   ip
        properties:
            ip:
                $ref: '#/definitions/IpAddr'
            name:
                description: Jeez! somebody forgot about it
                type: string
            vm_hostname:
                description: hostname assigned to this controller VM
                type: string
            vm_mor:
                description: Managed object reference of this controller VM
                type: string
            vm_name:
                description: Name of the controller VM
                type: string
            vm_uuid:
                description: UUID on the controller VM
                type: string
    ClusterNodeAddEvent:
        type: object
        properties:
            ip:
                $ref: '#/definitions/IpAddr'
            node_name:
                description: Name of controller node.
                type: string
            role:
                description: Role of the controller within the cluster.
                type: number
                format: int64
    ClusterNodeDbFailedEvent:
        type: object
        properties:
            failure_count:
                description: Number of failures.
                type: integer
                format: int32
            ip:
                $ref: '#/definitions/IpAddr'
            name:
                description: Name of controller node.
                type: string
    ClusterNodeRemoveEvent:
        type: object
        properties:
            ip:
                $ref: '#/definitions/IpAddr'
            node_name:
                description: Name of controller node.
                type: string
            role:
                description: Role of the node when it left the controller cluster.
                type: number
                format: int64
    ClusterServiceFailedEvent:
        type: object
        properties:
            node_name:
                description: Name of controller node.
                type: string
            service_name:
                description: Name of the controller service.
                type: string
    ClusterWarmRebootEvent:
        type: object
        properties:
    IpAddr:
        type: object
        required:
            -   addr
            -   type
        properties:
            addr:
                description: IP address
                type: string
            type:
                description: Jeez! somebody forgot about it
                type: number
                format: int64
