# GENERATED FILE - DO NOT EDIT THIS FILE
---
swagger: '2.0'
info:
    version: '16.3'
    title: Avi API
    contact:
        name: Avi Networks Inc.
        url: https://avinetworks.com/contact-us
        email: support@avinetworks.com
    description:  API documentation
securityDefinitions:
    basicAuth:
        type: basic
        description: basic authentication
basePath: /api
paths:
    /analytics/anomaly/pool/:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: aggregation
                    in: query
                    description: Enum options - METRICS_ANOMALY_AGG_NONE, METRICS_ANOMALY_AGG_COUNT
                    type: number
                -   name: aggregation_window
                    in: query
                    description: factor of aggregation window to the step. Eg. if requirement to is aggregate count ever 30 mins using 5 minute samples then value is 6.
                    type: integer
                -   name: detailed_header
                    in: query
                    description: ability to reduce the information returned in header
                    type: boolean
                -   name: dimension_filter_op
                    in: query
                    description: Filters the obj_id based on the filter specified.Enum options - METRICS_FILTER_EQUALS, METRICS_FILTER_LIKE, METRICS_FILTER_CONTAINS, METRICS_FILTER_NOT_EQUALS
                    type: number
                -   name: entity_uuid
                    in: query
                    description: uuid of the entity type
                    type: string
                -   name: include_refs
                    in: query
                    description: Resolve uuid to references
                    type: boolean
                -   name: include_related
                    in: query
                    description: include anomalies from related objects
                    type: boolean
                -   name: limit
                    in: query
                    description: Max number of metric samples
                    type: integer
                -   name: metric_entity
                    in: query
                    description: Metrics Types supported. Enum options - VSERVER_METRICS_ENTITY, VM_METRICS_ENTITY, SE_METRICS_ENTITY, CONTROLLER_METRICS_ENTITY, APPLICATION_METRICS_ENTITY, TENANT_METRICS_ENTITY
                    type: number
                -   name: metric_id
                    in: query
                    description: This is of the form l4_server. comma seperated values
                    type: string
                -   name: model
                    in: query
                    description: Enum options - EXPONENTIAL_MOVING_AVG, EXPONENTIAL_WEIGHTED_MOVING_AVG, HOLTWINTERS_AT_AS, HOLTWINTERS_AT_MS
                    type: number
                -   name: obj_id
                    in: query
                    description: object id like vip or server ip
                    type: string
                -   name: order_by
                    in: query
                    description: Positive means sort from start and Negative sign means sort from end. Only metric_timestamp is supported for now
                    type: string
                -   name: page
                    in: query
                    description: page id for the anomaly query
                    type: integer
                -   name: page_size
                    in: query
                    description: size of each page of anomalies
                    type: integer
                -   name: pool_uuid
                    in: query
                    description: Pool name. This will apply to any backend stats that are specified in the query
                    type: string
                -   name: priority
                    in: query
                    description: High priority Anomaly. Enum options - ANZ_PRIORITY_HIGH, ANZ_PRIORITY_MEDIUM, ANZ_PRIORITY_LOW
                    type: number
                -   name: result_format
                    in: query
                    description: format of the data returned. Enum options - METRICS_FORMAT_JSON, METRICS_FORMAT_CSV, METRICS_FORMAT_TXT, METRICS_FORMAT_PROTOBUF
                    type: number
                -   name: server
                    in: query
                    description: server ip port
                    type: string
                -   name: serviceengine_uuid
                    in: query
                    description: ID of the Node. Eg. if query is SE specific then it would have SE UUID
                    type: string
                -   name: start
                    in: query
                    description: Start time for the metrics in ISO 8601 format. Default is UNIX Epoch.
                    type: string
                -   name: step
                    in: query
                    description: step interval in seconds
                    type: integer
                -   name: stop
                    in: query
                    description: End time for the metrics in ISO 8601 format. Default is now. Negative numbers mean relative time from now
                    type: string
                -   name: tenant_uuid
                    in: query
                    description: UUID of the Tenant
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/AnomalyzerQueryResponseApiResponse"
                401:
                    description: log in failed
    /analytics/anomaly/pool/{uuid}:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: uuid
                    in: path
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/AnomalyzerQueryResponse"
                401:
                    description: log in failed
    /analytics/anomaly/serviceengine/:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: aggregation
                    in: query
                    description: Enum options - METRICS_ANOMALY_AGG_NONE, METRICS_ANOMALY_AGG_COUNT
                    type: number
                -   name: aggregation_window
                    in: query
                    description: factor of aggregation window to the step. Eg. if requirement to is aggregate count ever 30 mins using 5 minute samples then value is 6.
                    type: integer
                -   name: detailed_header
                    in: query
                    description: ability to reduce the information returned in header
                    type: boolean
                -   name: dimension_filter_op
                    in: query
                    description: Filters the obj_id based on the filter specified.Enum options - METRICS_FILTER_EQUALS, METRICS_FILTER_LIKE, METRICS_FILTER_CONTAINS, METRICS_FILTER_NOT_EQUALS
                    type: number
                -   name: entity_uuid
                    in: query
                    description: uuid of the entity type
                    type: string
                -   name: include_refs
                    in: query
                    description: Resolve uuid to references
                    type: boolean
                -   name: include_related
                    in: query
                    description: include anomalies from related objects
                    type: boolean
                -   name: limit
                    in: query
                    description: Max number of metric samples
                    type: integer
                -   name: metric_entity
                    in: query
                    description: Metrics Types supported. Enum options - VSERVER_METRICS_ENTITY, VM_METRICS_ENTITY, SE_METRICS_ENTITY, CONTROLLER_METRICS_ENTITY, APPLICATION_METRICS_ENTITY, TENANT_METRICS_ENTITY
                    type: number
                -   name: metric_id
                    in: query
                    description: This is of the form l4_server. comma seperated values
                    type: string
                -   name: model
                    in: query
                    description: Enum options - EXPONENTIAL_MOVING_AVG, EXPONENTIAL_WEIGHTED_MOVING_AVG, HOLTWINTERS_AT_AS, HOLTWINTERS_AT_MS
                    type: number
                -   name: obj_id
                    in: query
                    description: object id like vip or server ip
                    type: string
                -   name: order_by
                    in: query
                    description: Positive means sort from start and Negative sign means sort from end. Only metric_timestamp is supported for now
                    type: string
                -   name: page
                    in: query
                    description: page id for the anomaly query
                    type: integer
                -   name: page_size
                    in: query
                    description: size of each page of anomalies
                    type: integer
                -   name: pool_uuid
                    in: query
                    description: Pool name. This will apply to any backend stats that are specified in the query
                    type: string
                -   name: priority
                    in: query
                    description: High priority Anomaly. Enum options - ANZ_PRIORITY_HIGH, ANZ_PRIORITY_MEDIUM, ANZ_PRIORITY_LOW
                    type: number
                -   name: result_format
                    in: query
                    description: format of the data returned. Enum options - METRICS_FORMAT_JSON, METRICS_FORMAT_CSV, METRICS_FORMAT_TXT, METRICS_FORMAT_PROTOBUF
                    type: number
                -   name: server
                    in: query
                    description: server ip port
                    type: string
                -   name: serviceengine_uuid
                    in: query
                    description: ID of the Node. Eg. if query is SE specific then it would have SE UUID
                    type: string
                -   name: start
                    in: query
                    description: Start time for the metrics in ISO 8601 format. Default is UNIX Epoch.
                    type: string
                -   name: step
                    in: query
                    description: step interval in seconds
                    type: integer
                -   name: stop
                    in: query
                    description: End time for the metrics in ISO 8601 format. Default is now. Negative numbers mean relative time from now
                    type: string
                -   name: tenant_uuid
                    in: query
                    description: UUID of the Tenant
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/AnomalyzerQueryResponseApiResponse"
                401:
                    description: log in failed
    /analytics/anomaly/serviceengine/{uuid}:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: uuid
                    in: path
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/AnomalyzerQueryResponse"
                401:
                    description: log in failed
    /analytics/anomaly/virtualservice/:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: aggregation
                    in: query
                    description: Enum options - METRICS_ANOMALY_AGG_NONE, METRICS_ANOMALY_AGG_COUNT
                    type: number
                -   name: aggregation_window
                    in: query
                    description: factor of aggregation window to the step. Eg. if requirement to is aggregate count ever 30 mins using 5 minute samples then value is 6.
                    type: integer
                -   name: detailed_header
                    in: query
                    description: ability to reduce the information returned in header
                    type: boolean
                -   name: dimension_filter_op
                    in: query
                    description: Filters the obj_id based on the filter specified.Enum options - METRICS_FILTER_EQUALS, METRICS_FILTER_LIKE, METRICS_FILTER_CONTAINS, METRICS_FILTER_NOT_EQUALS
                    type: number
                -   name: entity_uuid
                    in: query
                    description: uuid of the entity type
                    type: string
                -   name: include_refs
                    in: query
                    description: Resolve uuid to references
                    type: boolean
                -   name: include_related
                    in: query
                    description: include anomalies from related objects
                    type: boolean
                -   name: limit
                    in: query
                    description: Max number of metric samples
                    type: integer
                -   name: metric_entity
                    in: query
                    description: Metrics Types supported. Enum options - VSERVER_METRICS_ENTITY, VM_METRICS_ENTITY, SE_METRICS_ENTITY, CONTROLLER_METRICS_ENTITY, APPLICATION_METRICS_ENTITY, TENANT_METRICS_ENTITY
                    type: number
                -   name: metric_id
                    in: query
                    description: This is of the form l4_server. comma seperated values
                    type: string
                -   name: model
                    in: query
                    description: Enum options - EXPONENTIAL_MOVING_AVG, EXPONENTIAL_WEIGHTED_MOVING_AVG, HOLTWINTERS_AT_AS, HOLTWINTERS_AT_MS
                    type: number
                -   name: obj_id
                    in: query
                    description: object id like vip or server ip
                    type: string
                -   name: order_by
                    in: query
                    description: Positive means sort from start and Negative sign means sort from end. Only metric_timestamp is supported for now
                    type: string
                -   name: page
                    in: query
                    description: page id for the anomaly query
                    type: integer
                -   name: page_size
                    in: query
                    description: size of each page of anomalies
                    type: integer
                -   name: pool_uuid
                    in: query
                    description: Pool name. This will apply to any backend stats that are specified in the query
                    type: string
                -   name: priority
                    in: query
                    description: High priority Anomaly. Enum options - ANZ_PRIORITY_HIGH, ANZ_PRIORITY_MEDIUM, ANZ_PRIORITY_LOW
                    type: number
                -   name: result_format
                    in: query
                    description: format of the data returned. Enum options - METRICS_FORMAT_JSON, METRICS_FORMAT_CSV, METRICS_FORMAT_TXT, METRICS_FORMAT_PROTOBUF
                    type: number
                -   name: server
                    in: query
                    description: server ip port
                    type: string
                -   name: serviceengine_uuid
                    in: query
                    description: ID of the Node. Eg. if query is SE specific then it would have SE UUID
                    type: string
                -   name: start
                    in: query
                    description: Start time for the metrics in ISO 8601 format. Default is UNIX Epoch.
                    type: string
                -   name: step
                    in: query
                    description: step interval in seconds
                    type: integer
                -   name: stop
                    in: query
                    description: End time for the metrics in ISO 8601 format. Default is now. Negative numbers mean relative time from now
                    type: string
                -   name: tenant_uuid
                    in: query
                    description: UUID of the Tenant
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/AnomalyzerQueryResponseApiResponse"
                401:
                    description: log in failed
    /analytics/anomaly/virtualservice/{uuid}:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: uuid
                    in: path
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/AnomalyzerQueryResponse"
                401:
                    description: log in failed
definitions:
    AnomalyContext:
        type: object
        properties:
            ema_context:
                $ref: '#/definitions/ExponentialMovingAverageCtx'
            ewma_context:
                $ref: '#/definitions/ExponentialMovingAverageCtx'
            hw_at_as_context:
                $ref: '#/definitions/HoltWintersCtx'
            hw_at_ms_context:
                $ref: '#/definitions/HoltWintersCtx'
            prediction_interval_high:
                description: ""
                type: number
                format: double
            prediction_interval_low:
                description: ""
                type: number
                format: double
    AnomalyData:
        type: object
        required:
            -   timestamp
            -   value
        properties:
            anomaly_context:
                $ref: '#/definitions/AnomalyContext'
            anomaly_model:
                description: "Deprecated. Enum options - EXPONENTIAL_MOVING_AVG, EXPONENTIAL_WEIGHTED_MOVING_AVG, HOLTWINTERS_AT_AS, HOLTWINTERS_AT_MS"
                type: number
                format: int64
            entity_uuid:
                description: ""
                type: string
            obj_id:
                description: ""
                type: string
            obj_id_type:
                description: "Object ID type. Enum options - METRICS_OBJ_ID_TYPE_VIRTUALSERVICE, METRICS_OBJ_ID_TYPE_SERVER, METRICS_OBJ_ID_TYPE_POOL, METRICS_OBJ_ID_TYPE_SERVICEENGINE, METRICS_OBJ_ID_TYPE_VIRTUALMACHINE, METRICS_OBJ_ID_TYPE_CONTROLLER, METRICS_OBJ_ID_TYPE_TENANT, METRICS_OBJ_ID_TYPE_CLUSTER, METRICS_OBJ_ID_TYPE_SE_INTERFACE"
                type: number
                format: int64
            pool_uuid:
                description: ""
                type: string
            priority:
                description: "Enum options - ANZ_PRIORITY_HIGH, ANZ_PRIORITY_MEDIUM, ANZ_PRIORITY_LOW"
                type: number
                format: int64
            timestamp:
                description: ""
                type: string
            value:
                description: ""
                type: number
                format: double
    AnomalyDataSeries:
        type: object
        required:
            -   header
        properties:
            data:
                type: array
                items:
                    $ref: '#/definitions/AnomalyData'
            header:
                $ref: '#/definitions/MetricsDataHeader'
    AnomalyzerQuery:
        type: object
        required:
            -   entity_uuid
            -   metric_entity
        properties:
            aggregation:
                description: "Enum options - METRICS_ANOMALY_AGG_NONE, METRICS_ANOMALY_AGG_COUNT"
                type: number
                format: int64
            aggregation_window:
                description: "factor of aggregation window to the step. Eg. if requirement to is aggregate count ever 30 mins using 5 minute samples then value is 6."
                type: integer
                format: int32
            detailed_header:
                description: "ability to reduce the information returned in header"
                type: boolean
            dimension_filter_op:
                description: "Filters the obj_id based on the filter specified.Enum options - METRICS_FILTER_EQUALS, METRICS_FILTER_LIKE, METRICS_FILTER_CONTAINS, METRICS_FILTER_NOT_EQUALS"
                type: number
                format: int64
            entity_uuid:
                description: "uuid of the entity type"
                type: string
            include_refs:
                description: "Resolve uuid to references"
                type: boolean
            include_related:
                description: "include anomalies from related objects"
                type: boolean
            limit:
                description: "Max number of metric samples"
                type: integer
                format: int32
            metric_entity:
                description: "Metrics Types supported. Enum options - VSERVER_METRICS_ENTITY, VM_METRICS_ENTITY, SE_METRICS_ENTITY, CONTROLLER_METRICS_ENTITY, APPLICATION_METRICS_ENTITY, TENANT_METRICS_ENTITY"
                type: number
                format: int64
            metric_id:
                description: "This is of the form l4_server. comma seperated values"
                type: string
            model:
                description: "Enum options - EXPONENTIAL_MOVING_AVG, EXPONENTIAL_WEIGHTED_MOVING_AVG, HOLTWINTERS_AT_AS, HOLTWINTERS_AT_MS"
                type: number
                format: int64
            obj_id:
                description: "object id like vip or server ip"
                type: string
            order_by:
                description: "Positive means sort from start and Negative sign means sort from end. Only metric_timestamp is supported for now"
                type: string
            page:
                description: "page id for the anomaly query"
                type: integer
                format: int32
            page_size:
                description: "size of each page of anomalies"
                type: integer
                format: int32
            pool_uuid:
                description: "Pool name. This will apply to any backend stats that are specified in the query"
                type: string
            priority:
                description: "High priority Anomaly. Enum options - ANZ_PRIORITY_HIGH, ANZ_PRIORITY_MEDIUM, ANZ_PRIORITY_LOW"
                type: number
                format: int64
            result_format:
                description: "format of the data returned. Enum options - METRICS_FORMAT_JSON, METRICS_FORMAT_CSV, METRICS_FORMAT_TXT, METRICS_FORMAT_PROTOBUF"
                type: number
                format: int64
            server:
                description: "server ip port"
                type: string
            serviceengine_uuid:
                description: "ID of the Node. Eg. if query is SE specific then it would have SE UUID"
                type: string
            start:
                description: "Start time for the metrics in ISO 8601 format. Default is UNIX Epoch."
                type: string
            step:
                description: "step interval in seconds"
                type: integer
                format: int32
            stop:
                description: "End time for the metrics in ISO 8601 format. Default is now. Negative numbers mean relative time from now"
                type: string
            tenant_uuid:
                description: "UUID of the Tenant"
                type: string
    AnomalyzerQueryResponse:
        type: object
        required:
            -   start
            -   step
            -   stop
        properties:
            metric_id:
                description: ""
                type: string
            series:
                type: array
                items:
                    $ref: '#/definitions/AnomalyDataSeries'
            start:
                description: ""
                type: string
            step:
                description: ""
                type: integer
                format: int32
            stop:
                description: ""
                type: string
            url:
                description: "url"
                readOnly: True
                type: string
    AnomalyzerQueryResponseApiResponse:
        type: object
        required:
            - count
            - results
        properties:
            count:
                type: integer
                format: int32
            results:
                type: array
                items:
                    $ref: "#/definitions/AnomalyzerQueryResponse"
    ExponentialMovingAverageCtx:
        type: object
        required:
            -   average
            -   deviation
            -   max_std_dev
            -   prediction
            -   std_dev
        properties:
            average:
                description: "Average value for the metric"
                type: number
                format: double
            deviation:
                description: "Deviation of last sample to the average"
                type: number
                format: double
            max_std_dev:
                description: "Maximum standard deviation recorded"
                type: number
                format: double
            normal_high:
                description: "Deprecated."
                type: number
                format: double
            normal_low:
                description: "Deprecated."
                type: number
                format: double
            prediction:
                description: "Predicted value for the metric at the time instant"
                type: number
                format: double
            prediction_interval_high:
                description: "Lower boundary of the range of normal values for this metric"
                type: number
                format: double
            prediction_interval_low:
                description: "Lower boundary of the range of normal values for this metric"
                type: number
                format: double
            std_dev:
                description: "Standard deviation"
                type: number
                format: double
    HoltWintersCtx:
        type: object
        properties:
            deviation:
                description: ""
                type: number
                format: double
            level:
                description: ""
                type: number
                format: double
            prediction:
                description: ""
                type: number
                format: double
            prediction_interval_high:
                description: ""
                type: number
                format: double
            prediction_interval_low:
                description: ""
                type: number
                format: double
            trend:
                description: ""
                type: number
                format: double
    MetricsDataHeader:
        type: object
        required:
            -   name
        properties:
            derivation_data:
                $ref: '#/definitions/MetricsDerivationData'
            dimension_data:
                type: array
                items:
                    $ref: '#/definitions/MetricsDimensionData'
            entity_uuid:
                description: "entity uuid"
                type: string
            metric_description:
                description: ""
                type: string
            metrics_min_scale:
                description: ""
                type: number
                format: double
            metrics_sum_agg_invalid:
                description: ""
                type: boolean
            missing_intervals:
                type: array
                items:
                    $ref: '#/definitions/MetricsMissingDataInterval'
            name:
                description: "name of the column"
                type: string
            obj_id:
                description: "object ID of the series when object ID was specified in the metric"
                type: string
            obj_id_type:
                description: "obj_id_type. Enum options - METRICS_OBJ_ID_TYPE_VIRTUALSERVICE, METRICS_OBJ_ID_TYPE_SERVER, METRICS_OBJ_ID_TYPE_POOL, METRICS_OBJ_ID_TYPE_SERVICEENGINE, METRICS_OBJ_ID_TYPE_VIRTUALMACHINE, METRICS_OBJ_ID_TYPE_CONTROLLER, METRICS_OBJ_ID_TYPE_TENANT, METRICS_OBJ_ID_TYPE_CLUSTER, METRICS_OBJ_ID_TYPE_SE_INTERFACE"
                type: number
                format: int64
            pool_uuid:
                description: "pool_id for the metric"
                type: string
            priority:
                description: ""
                type: boolean
            server:
                description: "server ip port"
                type: string
            statistics:
                $ref: '#/definitions/MetricStatistics'
            tenant_uuid:
                description: "Tenant UUID"
                type: string
            units:
                description: "units of the column data. Enum options - METRIC_COUNT, BITS_PER_SECOND, MILLISECONDS, SECONDS, PER_SECOND, BYTES, PERCENT, KILO_BYTES, KILO_BYTES_PER_SECOND, BYTES_PER_SECOND, KILO_BITS_PER_SECOND, GIGA_BYTES, MEGA_BYTES, NORMALIZED"
                type: number
                format: int64
    MetricsDerivationData:
        type: object
        required:
            -   derivation_fn
            -   metric_ids
        properties:
            derivation_fn:
                description: "Enum options - METRICS_ALIAS, SUM_FIRST_N_DIVIDE_BY_LAST, SUM_BW_GAUGE, AVG_GET_POST_OTHER_LATENCY, APPDEX_ON_3_BUCKETS, APPDEX_ON_4_BUCKETS, SUM_GAUGE, SUM_N_METRICS, APPDEX_ON_5_BUCKETS, APPDEX_ON_6_BUCKETS, APPDEX_ON_CONNECTIONS, APPDEX_ON_2_BUCKETS, AVG_CLIENT_LATENCY, AVG_APPLICATION_LATENCY, MIN_N_METRICS, SUM_FIRST_N_DIVIDE_BY_LAST_PERCENTAGE, L4_CONNECTION_ERROR_PERCENTAGE, AVG_L4_CLIENT_LATENCY, CHECK_FOR_TRANSITIONS, SUBSTRACT_ALL_FROM_FIRST, AVG_N_OVER_TIME_PERIOD, AVG_NAVIGATION_TIMING, AVG_RUM_VISITS, PCT_SSL_ERROR_CONNECTIONS, AVG_RESPONSE_TIME, SUM_RATES_FIRST_N_DIVIDE_BY_LAST, SUM_RATES_FIRST_N_DIVIDE_BY_LAST_PERCENTAGE, PCT_CACHE_METRICS, SUM_FIRST_N_DIVIDE_BY_SECLAST_EXCL_ERROR_RATE, SUM_FIRST_N_SUBSTRACT_LAST, AVG_POOL_METRICS, AVG_POOL_BW, AVG_BY_SUBSTRACT_ALL_FROM_FIRST_OVER_TIME, AVG_RSA_PFS, EVAL_FN, SSL_PROTOCOL_INDICATOR, SUM_FIRST_N_DIVIDE_BY_SECLAST_RATE_EXCL_ERROR_RATE, SUBSTRACT_ALL_FROM_FIRST_WITH_FLOOR_ZERO, AVAILABLE_CAPACITY, CONNECTION_SATURATION, AVG_RSA_NON_PFS, SSL_HANDSHAKES_NONPFS"
                type: number
                format: int64
            exclude_derived_metric:
                description: ""
                type: boolean
            include_derivation_metrics:
                description: ""
                type: boolean
            join_tables:
                description: "Enum options - METRICS_TABLE_NONE, METRICS_TABLE_ANOMALY, METRICS_TABLE_CONTROLLER_STATS, METRICS_TABLE_HEALTH_SCORE, METRICS_TABLE_SE_STATS, METRICS_TABLE_VSERVER_L4_SERVER, METRICS_TABLE_VSERVER_L4_CLIENT, METRICS_TABLE_VSERVER_L7_CLIENT, METRICS_TABLE_VSERVER_L7_SERVER, METRICS_TABLE_RUM_PREAGG_BROWSER, METRICS_TABLE_RUM_PREAGG_COUNTRY, METRICS_TABLE_RUM_PREAGG_DEVTYPE, METRICS_TABLE_RUM_PREAGG_LANG, METRICS_TABLE_RUM_PREAGG_OS, METRICS_TABLE_RUM_PREAGG_URL, METRICS_TABLE_RUM_ANALYTICS, METRICS_TABLE_VM_STATS, METRICS_TABLE_RESOURCE_TIMING_DIM, METRICS_TABLE_RESOURCE_TIMING_BLOB, METRICS_TABLE_RUM_PREAGG_IPGROUP, METRICS_TABLE_DOS_ANALYTICS, METRICS_TABLE_APP_INSIGHTS, METRICS_TABLE_VSERVER_DNS, METRICS_TABLE_SERVER_DNS, METRICS_TABLE_SERVICE_INSIGHTS, METRICS_TABLE_SOURCE_INSIGHTS, METRICS_TABLE_TENANT_STATS, METRICS_TABLE_SE_IF_STATS"
                type: number
                format: int64
            metric_ids:
                description: ""
                type: string
            result_has_additional_fields:
                description: ""
                type: boolean
            skip_backend_derivation:
                description: ""
                type: boolean
    MetricsDimensionData:
        type: object
        required:
            -   dimension
            -   dimension_id
        properties:
            dimension:
                description: "Dimension Type. Enum options - METRICS_DIMENSION_METRIC_TIMESTAMP, METRICS_DIMENSION_COUNTRY, METRICS_DIMENSION_OS, METRICS_DIMENSION_URL, METRICS_DIMENSION_DEVTYPE, METRICS_DIMENSION_LANG, METRICS_DIMENSION_BROWSER, METRICS_DIMENSION_IPGROUP, METRICS_DIMENSION_ATTACK, METRICS_DIMENSION_ASN"
                type: number
                format: int64
            dimension_id:
                description: "Dimension ID"
                type: string
    MetricsMissingDataInterval:
        type: object
        required:
            -   end
            -   start
        properties:
            end:
                description: ""
                type: string
            start:
                description: ""
                type: string
    MetricStatistics:
        type: object
        properties:
            last_sample:
                description: "value of the last sample"
                type: number
                format: double
            max:
                description: "maximum value in time series requested"
                type: number
                format: double
            max_ts:
                description: "timestamp of the minimum value"
                type: string
            mean:
                description: "arithmetic mean"
                type: number
                format: double
            min:
                description: "minimum value in time series requested"
                type: number
                format: double
            min_ts:
                description: "timestamp of the minimum value"
                type: string
            num_samples:
                description: "Number of actual data samples. It excludes fake data"
                type: integer
                format: int32
            sum:
                description: "summation of all values"
                type: number
                format: double
            trend:
                description: "slope of the data points across the time series requested. trend = (last_value - avg)/avg"
                type: number
                format: double
