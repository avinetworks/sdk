# GENERATED FILE - DO NOT EDIT THIS FILE
---
swagger: '2.0'
info:
    version: '16.3'
    title: Avi API
schemes:
    -   https
securityDefinitions:
    basicAuth:
        type: basic
        description: basic authentication
basePath: /api
paths:
    /virtualservice:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/VirtualServiceApiResponse"
                401:
                    description: log in failed                     
        post:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: VirtualService object creation
                    required: true
                    schema:
                        $ref: "#/definitions/VirtualService"
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/VirtualService"
                401:
                    description: log in failed
    /virtualservice/{uuid}:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: uuid
                    in: path
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/VirtualService"
                401:
                    description: log in failed
        put:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: VirtualService object creation
                    required: true
                    schema:
                        $ref: "#/definitions/VirtualService"
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/VirtualService"
                401:
                    description: log in failed
        patch:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: VirtualService object creation
                    required: true
                    schema:
                        $ref: "#/definitions/VirtualService"
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/VirtualService"
                401:
                    description: log in failed
        delete:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                204:
                    description: object deleted
                    schema:
                        type: string
                404:
                    description: not found
definitions:
    AnalyticsPolicy:
        type: object
        properties:
            client_insights:
                description: Gain insights from sampled client to server HTTP requests and responses.
                type: number
                format: int64
            client_insights_sampling:
                $ref: '#/definitions/ClientInsightsSampling'
            client_log_filters:
                type: array
                items:
                    $ref: '#/definitions/ClientLogFilter'
            full_client_logs:
                $ref: '#/definitions/FullClientLogs'
            metrics_realtime_update:
                $ref: '#/definitions/MetricsRealTimeUpdate'
    ClientInsightsSampling:
        type: object
        properties:
            client_ip:
                $ref: '#/definitions/IpAddrMatch'
            sample_uris:
                $ref: '#/definitions/StringMatch'
            skip_uris:
                $ref: '#/definitions/StringMatch'
    ClientLogFilter:
        type: object
        required:
            -   enabled
            -   index
            -   name
        properties:
            all_headers:
                description: Jeez! somebody forgot about it
                type: boolean
            client_ip:
                $ref: '#/definitions/IpAddrMatch'
            duration:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            enabled:
                description: Jeez! somebody forgot about it
                type: boolean
            index:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            name:
                description: Jeez! somebody forgot about it
                type: string
            uri:
                $ref: '#/definitions/StringMatch'
    DiscoveredNetwork:
        type: object
        required:
            -   network_ref
        properties:
            network_ref:
                description: Discovered network for this IP. object ref Network.
                type: string
            subnet:
                type: array
                items:
                    $ref: '#/definitions/IpAddrPrefix'
    EquivalentLabels:
        type: object
        properties:
            labels:
                description: Equivalent labels.
                type: array
                items:
                    type: string
    FullClientLogs:
        type: object
        required:
            -   enabled
        properties:
            duration:
                description: How long should the system capture all logs, measured in minutes. Set to 0 for infinite.
                type: integer
                format: int32
            enabled:
                description: Capture all client logs including connections and requests.  When disabled, only errors will be logged.
                type: boolean
    HTTPClientAuthenticationParams:
        type: object
        properties:
            auth_profile_ref:
                description: Auth Profile to use for validating users object ref AuthProfile.
                type: string
            realm:
                description: Basic authentication realm to present to a user along with the prompt for credentials.
                type: string
            request_uri_path:
                $ref: '#/definitions/StringMatch'
            type:
                description: type of client authentication
                type: number
                format: int64
    HTTPLocalFile:
        type: object
        required:
            -   content_type
            -   file_content
        properties:
            content_type:
                description: Mime-type of the content in the file.
                type: string
            file_content:
                description: File content to used in the local HTTP response body.
                type: string
    HTTPPolicies:
        type: object
        required:
            -   http_policy_set_ref
            -   index
        properties:
            http_policy_set_ref:
                description: UUID of the virtual service HTTP policy collection object ref HTTPPolicySet.
                type: string
            index:
                description: Index of the virtual service HTTP policy collection
                type: integer
                format: int32
    HTTPRedirectAction:
        type: object
        required:
            -   protocol
        properties:
            host:
                $ref: '#/definitions/URIParam'
            keep_query:
                description: Keep or drop the query of the incoming request URI in the redirected URI
                type: boolean
            path:
                $ref: '#/definitions/URIParam'
            port:
                description: Port to which redirect the request
                type: integer
                format: int32
            protocol:
                description: Protocol type
                type: number
                format: int64
            status_code:
                description: HTTP redirect status code
                type: number
                format: int64
    IpAddr:
        type: object
        required:
            -   addr
            -   type
        properties:
            addr:
                description: IP address
                type: string
            type:
                description: Jeez! somebody forgot about it
                type: number
                format: int64
    IpAddrMatch:
        type: object
        required:
            -   match_criteria
        properties:
            addrs:
                type: array
                items:
                    $ref: '#/definitions/IpAddr'
            group_refs:
                description: UUID of IP address group(s) object ref IpAddrGroup.
                type: array
                items:
                    type: string
            match_criteria:
                description: Criterion to use for IP address matching the HTTP request
                type: number
                format: int64
            prefixes:
                type: array
                items:
                    $ref: '#/definitions/IpAddrPrefix'
            ranges:
                type: array
                items:
                    $ref: '#/definitions/IpAddrRange'
    IpAddrPrefix:
        type: object
        required:
            -   ip_addr
            -   mask
        properties:
            ip_addr:
                $ref: '#/definitions/IpAddr'
            mask:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
    IpAddrRange:
        type: object
        required:
            -   begin
            -   end
        properties:
            begin:
                $ref: '#/definitions/IpAddr'
            end:
                $ref: '#/definitions/IpAddr'
    MetricsRealTimeUpdate:
        type: object
        required:
            -   enabled
        properties:
            duration:
                description: Real time metrics collection duration in minutes. 0 for infinite.
                type: integer
                format: int32
            enabled:
                description: Enables real time metrics collection.  When disabled, 6 hour view is the most granular the system will track.
                type: boolean
    PerformanceLimits:
        type: object
        properties:
            max_concurrent_connections:
                description: The maximum number of concurrent client conections allowed to the Virtual Service.
                type: integer
                format: int32
            max_throughput:
                description: The maximum throughput per second for all clients allowed through the client side of the Virtual Service.
                type: integer
                format: int32
    RateLimiterAction:
        type: object
        properties:
            file:
                $ref: '#/definitions/HTTPLocalFile'
            redirect:
                $ref: '#/definitions/HTTPRedirectAction'
            status_code:
                description: HTTP status code for Local Response rate limit action.
                type: number
                format: int64
            type:
                description: Type of action to be enforced upon hitting the rate limit.
                type: number
                format: int64
    RateProfile:
        type: object
        properties:
            action:
                $ref: '#/definitions/RateLimiterAction'
            burst_sz:
                description: Maximum number of connections or requests or packets to be let through instantaneously
                type: integer
                format: int32
            count:
                description: Maximum number of connections or requests or packets
                type: integer
                format: int32
            explicit_tracking:
                description: Explicitly tracks an attacker across rate periods
                type: boolean
            fine_grain:
                description: Enable fine granularity
                type: boolean
            period:
                description: Time value in seconds to enforce rate count
                type: integer
                format: int32
    SeList:
        type: object
        required:
            -   delete_in_progress
            -   is_connected
            -   is_primary
            -   memory
            -   se_ref
            -   vcpus
            -   vip_intf_mac
            -   vip_subnet_mask
        properties:
            delete_in_progress:
                description: Jeez! somebody forgot about it
                type: boolean
            download_selist_only:
                description: Jeez! somebody forgot about it
                type: boolean
            equivalent_labels:
                type: array
                items:
                    $ref: '#/definitions/EquivalentLabels'
            floating_intf_ip:
                type: array
                items:
                    $ref: '#/definitions/IpAddr'
            is_connected:
                description: Jeez! somebody forgot about it
                type: boolean
            is_portchannel:
                description: Jeez! somebody forgot about it
                type: boolean
            is_primary:
                description: Jeez! somebody forgot about it
                type: boolean
            is_standby:
                description: Jeez! somebody forgot about it
                type: boolean
            memory:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            pending_download:
                description: Jeez! somebody forgot about it
                type: boolean
            se_flow_label:
                description: SE label for vs.
                type: string
            se_ref:
                description: Jeez! somebody forgot about it object ref ServiceEngine.
                type: string
            sec_idx:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            snat_ip:
                $ref: '#/definitions/IpAddr'
            vcpus:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            vip_intf_ip:
                $ref: '#/definitions/IpAddr'
            vip_intf_list:
                type: array
                items:
                    $ref: '#/definitions/SeVipInterfaceList'
            vip_intf_mac:
                description: Jeez! somebody forgot about it
                type: string
            vip_subnet_mask:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            vlan_id:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            vnic:
                type: array
                items:
                    $ref: '#/definitions/VsSeVnic'
    Service:
        type: object
        required:
            -   port
        properties:
            enable_ssl:
                description: Enable SSL termination and offload for traffic from clients.
                type: boolean
            override_network_profile_ref:
                description: Override the network profile for this specific service port. object ref NetworkProfile.
                type: string
            port:
                description: The Virtual Service's port number.
                type: integer
                format: int32
            port_range_end:
                description: The end of the Virtual Service's port number range.
                type: integer
                format: int32
    ServicePoolSelector:
        type: object
        required:
            -   service_pool_ref
            -   service_port
        properties:
            service_pool_ref:
                description: Jeez! somebody forgot about it object ref Pool.
                type: string
            service_port:
                description: Pool based destination port
                type: integer
                format: int32
    SeVipInterfaceList:
        type: object
        required:
            -   vip_intf_mac
        properties:
            is_portchannel:
                description: Jeez! somebody forgot about it
                type: boolean
            vip_intf_ip:
                $ref: '#/definitions/IpAddr'
            vip_intf_mac:
                description: Jeez! somebody forgot about it
                type: string
            vlan_id:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
    StringMatch:
        type: object
        required:
            -   match_criteria
        properties:
            match_criteria:
                description: Criterion to use for string matching the HTTP request
                type: number
                format: int64
            match_str:
                description: String value(s)
                type: array
                items:
                    type: string
            string_group_refs:
                description: UUID of the string group(s) object ref StringGroup.
                type: array
                items:
                    type: string
    TLSTicket:
        type: object
        required:
            -   aes_key
            -   hmac_key
            -   name
        properties:
            aes_key:
                description: Jeez! somebody forgot about it
                type: string
            hmac_key:
                description: Jeez! somebody forgot about it
                type: string
            name:
                description: Jeez! somebody forgot about it
                type: string
    URIParam:
        type: object
        required:
            -   type
        properties:
            tokens:
                type: array
                items:
                    $ref: '#/definitions/URIParamToken'
            type:
                description: URI param type
                type: number
                format: int64
    URIParamToken:
        type: object
        required:
            -   type
        properties:
            end_index:
                description: Index of the ending token in the incoming URI
                type: integer
                format: int32
            start_index:
                description: Index of the starting token in the incoming URI
                type: integer
                format: int32
            str_value:
                description: Constant string to use as a token
                type: string
            type:
                description: Token type for constructing the URI
                type: number
                format: int64
    VirtualService:
        type: object
        required:
            -   name
        properties:
            active_standby_se_tag:
                description: This configuration only applies if the VirtualService is in Legacy Active Standby HA mode and Load Distribution among Active Standby is enabled. This field is used to tag the VirtualService so that VirtualServices with the same tag will share the same Active ServiceEngine and VirtualServices with different tags will have different Active ServiceEngines. If one of the ServiceEngine's in the ServiceEngineGroup fails, all VirtualServices will end up using the same Active ServiceEngine. Redistribution of the VirtualServices, once the failed ServiceEngine recovers can either be manual or automated, based on the auto redistribute property of the ServiceEngineGroup
                type: number
                format: int64
            analytics_policy:
                $ref: '#/definitions/AnalyticsPolicy'
            analytics_profile_ref:
                description: Specifies settings related to analytics. object ref AnalyticsProfile.
                type: string
            application_profile_ref:
                description: Enable application layer specific features for the Virtual Service. object ref ApplicationProfile.
                type: string
            auto_allocate_floating_ip:
                description: Auto-allocate floating/elastic IP from the Cloud infrastructure.
                type: boolean
            auto_allocate_ip:
                description: Auto-allocate VIP from the provided subnet.
                type: boolean
            availability_zone:
                description: Availability-zone to place the Virtual Service.
                type: string
            avi_allocated_fip:
                description: (internal-use) FIP allocated by Avi in the Cloud infrastructure.
                type: boolean
            avi_allocated_vip:
                description: (internal-use) VIP allocated by Avi in the Cloud infrastructure.
                type: boolean
            client_auth:
                $ref: '#/definitions/HTTPClientAuthenticationParams'
            cloud_config_cksum:
                description: Checksum of cloud configuration for VS. Internally set by cloud connector
                type: string
            cloud_ref:
                description: Jeez! somebody forgot about it object ref Cloud.
                readOnly: True
                type: string
            cloud_type:
                description: Jeez! somebody forgot about it
                type: number
                format: int64
            connections_rate_limit:
                $ref: '#/definitions/RateProfile'
            created_by:
                description: Creator name
                type: string
            delay_fairness:
                description: Select the algorithm for QoS fairness.  This determines how multiple Virtual Services sharing the same Service Engines will prioritize traffic over a congested network.
                type: boolean
            description:
                description: Jeez! somebody forgot about it
                type: string
            discovered_network_ref:
                description: (internal-use) Discovered networks providing reachability for client facing Virtual Service IP. This field is deprecated. object ref Network.
                type: array
                items:
                    type: string
            discovered_networks:
                type: array
                items:
                    $ref: '#/definitions/DiscoveredNetwork'
            discovered_subnet:
                type: array
                items:
                    $ref: '#/definitions/IpAddrPrefix'
            east_west_placement:
                description: Force placement on all SE's in service group (Mesos mode only)
                type: boolean
            enable_autogw:
                description: Response traffic to clients will be sent back to the source MAC address of the connection, rather than statically sent to a default gateway.
                type: boolean
            enable_rhi:
                description: Enable Route Health Injection using the BGP Config in the vrf context
                type: boolean
            enabled:
                description: Enable or disable the Virtual Service.
                type: boolean
            floating_ip:
                $ref: '#/definitions/IpAddr'
            floating_subnet_uuid:
                description: If auto_allocate_floating_ip is True and more than one floating-ip subnets exist, then the subnet for the floating IP address allocation. This field is applicable only if the VirtualService belongs to an Openstack or AWS cloud, in which case it is mandatory, if auto_allocate_floating_ip is selected.
                type: string
            flow_dist:
                description: Criteria for flow distribution among SEs.
                type: number
                format: int64
            flow_label_type:
                description: Criteria for flow labelling.
                type: number
                format: int64
            fqdn:
                description: DNS resolvable, fully qualified domain name of the Virtual Service.
                type: string
            host_name_xlate:
                description: Translate the host name sent to the servers to this value.  Translate the host name sent from servers back to the value used by the client.
                type: string
            http_policies:
                type: array
                items:
                    $ref: '#/definitions/HTTPPolicies'
            ign_pool_net_reach:
                description: Ignore Pool servers network reachability constraints for Virtual Service placement.
                type: boolean
            ip_address:
                $ref: '#/definitions/IpAddr'
            limit_doser:
                description: Limit potential DoS attackers who exceed max_cps_per_client significantly to a fraction of max_cps_per_client for a while.
                type: boolean
            max_cps_per_client:
                description: Maximum connections per second per client IP.
                type: integer
                format: int32
            microservice_ref:
                description: Microservice representing the virtual service object ref MicroService.
                type: string
            name:
                description: Name for the Virtual Service.
                type: string
            network_profile_ref:
                description: Determines network settings such as protocol, TCP or UDP, and related options for the protocol. object ref NetworkProfile.
                type: string
            network_ref:
                description: Manually override the network on which the Virtual Service is placed. object ref Network.
                type: string
            network_security_policy_ref:
                description: Network security policies for the Virtual Service. object ref NetworkSecurityPolicy.
                type: string
            performance_limits:
                $ref: '#/definitions/PerformanceLimits'
            pool_group_ref:
                description: The pool group is an object that contains pools. object ref PoolGroup.
                type: string
            pool_ref:
                description: The pool is an object that contains destination servers and related attributes such as load-balancing and persistence. object ref Pool.
                type: string
            port_uuid:
                description: (internal-use) Network port assigned to the Virtual Service IP address.
                type: string
            remove_listening_port_on_vs_down:
                description: Remove listening port if VirtualService is down
                type: boolean
            requests_rate_limit:
                $ref: '#/definitions/RateProfile'
            scaleout_ecmp:
                description: Jeez! somebody forgot about it
                type: boolean
            se_group_ref:
                description: The Service Engine Group to use for this Virtual Service. Moving to a new SE Group is disruptive to existing connection for this VS. object ref ServiceEngineGroup.
                type: string
            server_network_profile_ref:
                description: Determines the network settings profile for the server side of TCP proxied connections.  Leave blank to use the same settings as the client to VS side of the connection. object ref NetworkProfile.
                type: string
            service_pool_select:
                type: array
                items:
                    $ref: '#/definitions/ServicePoolSelector'
            services:
                type: array
                items:
                    $ref: '#/definitions/Service'
            snat_ip:
                type: array
                items:
                    $ref: '#/definitions/IpAddr'
            ssl_key_and_certificate_refs:
                description: Select or create one or two certificates, EC and/or RSA, that will be presented to SSL/TLS terminated connections. object ref SSLKeyAndCertificate.
                type: array
                items:
                    type: string
            ssl_profile_ref:
                description: Determines the set of SSL versions and ciphers to accept for SSL/TLS terminated connections. object ref SSLProfile.
                type: string
            ssl_sess_cache_avg_size:
                description: Expected number of SSL session cache entries (may be exceeded).
                type: integer
                format: int32
            subnet:
                $ref: '#/definitions/IpAddrPrefix'
            subnet_uuid:
                description: If auto_allocate_ip is True, then the subnet for the Virtual Service IP address allocation. This field is applicable only if the VirtualService belongs to an Openstack or AWS cloud, in which case it is mandatory, if auto_allocate is selected.
                type: string
            tenant_ref:
                description: Jeez! somebody forgot about it object ref Tenant.
                type: string
            type:
                description: Specify if this is a normal Virtual Service, or if it is the parent or child of an SNI-enabled virtual hosted Virtual Service.
                type: number
                format: int64
            url:
                description: url
                readOnly: True
                type: string
            use_bridge_ip_as_vip:
                description: Use Bridge IP as VIP on each Host in Mesos deployments
                type: boolean
            uuid:
                description: UUID of the VirtualService.
                type: string
            vh_domain_name:
                description: The exact name requested from the client's SNI-enabled TLS hello domain name field. If this is a match, the parent VS will forward the connection to this child VS.
                type: array
                items:
                    type: string
            vh_parent_vs_uuid:
                description: Specifies the Virtual Service acting as Virtual Hosting (SNI) parent.
                type: string
            vrf_context_ref:
                description: Virtual Routing Context that the Virtual Service is bound to. This is used to provide the isolation of the set of networks the application is attached to. object ref VrfContext.
                type: string
            vs_datascripts:
                type: array
                items:
                    $ref: '#/definitions/VSDataScripts'
            weight:
                description: The Quality of Service weight to assign to traffic transmitted from this Virtual Service.  A higher weight will prioritize traffic versus other Virtual Services sharing the same Service Engines. (1-2-4-8)
                type: integer
                format: int32
    VirtualServiceApiResponse:
        type: object
        required:
            - count
            - results
        properties:
            count:
                type: integer
                format: int32
            results:
                type: array
                items:
                    $ref: "#/definitions/VirtualService"
    VirtualServiceResource:
        type: object
        properties:
            is_exclusive:
                description: Jeez! somebody forgot about it
                type: boolean
            memory:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            num_se:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            num_standby_se:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            num_vcpus:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            scalein_primary:
                description: Jeez! somebody forgot about it
                type: boolean
            scalein_se_uuid:
                description: Jeez! somebody forgot about it
                type: string
    VsApicExtension:
        type: object
        required:
            -   txn_uuid
        properties:
            se_uuid:
                description: Jeez! somebody forgot about it
                type: string
            txn_uuid:
                description: Jeez! somebody forgot about it
                type: string
            vnic:
                type: array
                items:
                    $ref: '#/definitions/VsSeVnic'
    VSDataScripts:
        type: object
        required:
            -   index
            -   vs_datascript_set_ref
        properties:
            index:
                description: Index of the virtual service datascript collection
                type: integer
                format: int32
            vs_datascript_set_ref:
                description: UUID of the virtual service datascript collection object ref VSDataScriptSet.
                type: string
    VsInitialPlacementParams:
        type: object
        properties:
            se_placement_params:
                type: array
                items:
                    $ref: '#/definitions/VsSeInitialPlacementParams'
    VsMigrateParams:
        type: object
        properties:
            from_se_ref:
                description: Jeez! somebody forgot about it object ref ServiceEngine.
                type: string
            new_vcpus:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            to_host_ref:
                description: Jeez! somebody forgot about it object ref VIMgrHostRuntime.
                type: string
            to_new_se:
                description: Jeez! somebody forgot about it
                type: boolean
            to_se_ref:
                description: Jeez! somebody forgot about it object ref ServiceEngine.
                type: string
    VsScaleinParams:
        type: object
        properties:
            admin_down:
                description: Jeez! somebody forgot about it
                type: boolean
            from_se_ref:
                description: Jeez! somebody forgot about it object ref ServiceEngine.
                type: string
            scalein_primary:
                description: Jeez! somebody forgot about it
                type: boolean
    VsScaleoutParams:
        type: object
        properties:
            admin_up:
                description: Jeez! somebody forgot about it
                type: boolean
            new_vcpus:
                description: Jeez! somebody forgot about it
                type: integer
                format: int32
            to_host_ref:
                description: Jeez! somebody forgot about it object ref VIMgrHostRuntime.
                type: string
            to_new_se:
                description: Jeez! somebody forgot about it
                type: boolean
            to_se_ref:
                description: Jeez! somebody forgot about it object ref ServiceEngine.
                type: string
    VsSeInitialPlacementParams:
        type: object
        properties:
            to_host_ref:
                description: Jeez! somebody forgot about it object ref VIMgrHostRuntime.
                type: string
            to_new_se:
                description: Jeez! somebody forgot about it
                type: boolean
            to_se_ref:
                description: Jeez! somebody forgot about it object ref ServiceEngine.
                type: string
    VsSeVnic:
        type: object
        required:
            -   mac
            -   type
        properties:
            lif:
                description: Jeez! somebody forgot about it
                type: string
            mac:
                description: Jeez! somebody forgot about it
                type: string
            type:
                description: Jeez! somebody forgot about it
                type: number
                format: int64
