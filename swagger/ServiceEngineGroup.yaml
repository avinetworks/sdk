# GENERATED FILE - DO NOT EDIT THIS FILE
---
swagger: '2.0'
info:
    version: '16.3'
    title: Avi API
    contact:
        name: Avi Networks Inc.
        url: https://avinetworks.com/contact-us
        email: support@avinetworks.com
    description: ServiceEngineGroup API documentation
securityDefinitions:
    basicAuth:
        type: basic
        description: basic authentication
basePath: /api
paths:
    /serviceenginegroup:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/ServiceEngineGroupApiResponse"
                401:
                    description: log in failed
        post:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: ServiceEngineGroup object creation
                    required: true
                    schema:
                        $ref: "#/definitions/ServiceEngineGroup"
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/ServiceEngineGroup"
                401:
                    description: log in failed
    /serviceenginegroup/{uuid}:
        get:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   name: name
                    in: query
                    description: object name
                    required: false
                    type: string
                -   name: X-Avi-Tenant
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   name: uuid
                    in: path
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/ServiceEngineGroup"
                401:
                    description: log in failed
        put:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: ServiceEngineGroup object creation
                    required: true
                    schema:
                        $ref: "#/definitions/ServiceEngineGroup"
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/ServiceEngineGroup"
                401:
                    description: log in failed
        patch:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: ServiceEngineGroup object creation
                    required: true
                    schema:
                        $ref: "#/definitions/ServiceEngineGroup"
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        $ref: "#/definitions/ServiceEngineGroup"
                401:
                    description: log in failed
        delete:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: query
                    name: name
                    description: object name
                    required: false
                    type: string
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                204:
                    description: object deleted
                    schema:
                        type: string
                404:
                    description: not found

    /serviceenginegroup/{uuid}/redistribute:
        post:
            consumes:
                -   application/json
            produces:
                -   application/json
            parameters:
                -   in: header
                    name: X-Avi-Tenant
                    type: string
                    required: false
                    description: Avi Tenant Header
                -   name: X-Avi-Tenant-UUID
                    in: header
                    type: string
                    required: false
                    description: Avi Tenant Header UUID
                -   name: X-CSRFToken
                    in: header
                    type: string
                    required: false
                    description: Avi Controller may send back CSRF token in the response cookies. The caller should update the request headers with this token else controller with reject requests.
                -   in: body
                    name: body
                    description: empty
                    required: true
                    schema:
                        type: object
                        additionalProperties:
                            type: string
                -   in: path
                    name: uuid
                    description: UUID of the object to fetch
                    required: true
                    type: string
            security:
                - basicAuth: []
            responses:
                200:
                    description: OK
                    schema:
                        type: string
                401:
                    description: log in failed
definitions:
    CustomTag:
        type: object
        required:
            -   tag_key
        properties:
            tag_key:
                description: ""
                type: string
            tag_val:
                description: ""
                type: string
    DosThreshold:
        type: object
        required:
            -   attack
            -   max_value
            -   min_value
        properties:
            attack:
                description: "Attack type.Enum options - LAND, SMURF, ICMP_PING_FLOOD, UNKOWN_PROTOCOL, TEARDROP, IP_FRAG_OVERRUN, IP_FRAG_TOOSMALL, IP_FRAG_FULL, IP_FRAG_INCOMPLETE, PORT_SCAN, TCP_NON_SYN_FLOOD_OLD, SYN_FLOOD, BAD_RST_FLOOD, MALFORMED_FLOOD, FAKE_SESSION, ZERO_WINDOW_STRESS, SMALL_WINDOW_STRESS, DOS_HTTP_TIMEOUT, DOS_HTTP_ERROR, DOS_HTTP_ABORT, DOS_SSL_ERROR, DOS_APP_ERROR, DOS_REQ_IP_RL_DROP, DOS_REQ_URI_RL_DROP, DOS_REQ_URI_SCAN_BAD_RL_DROP, DOS_REQ_URI_SCAN_UNKNOWN_RL_DROP, DOS_REQ_IP_URI_RL_DROP, DOS_CONN_IP_RL_DROP, DOS_SLOW_URL, TCP_NON_SYN_FLOOD, DOS_REQ_CIP_SCAN_BAD_RL_DROP, DOS_REQ_CIP_SCAN_UNKNOWN_RL_DROP, DOS_REQ_IP_RL_DROP_BAD, DOS_REQ_URI_RL_DROP_BAD, DOS_REQ_IP_URI_RL_DROP_BAD, POLICY_DROPS, DOS_CONN_RL_DROP, DOS_REQ_RL_DROP"
                type: number
                format: int64
            max_value:
                description: "Maximum number of packets or connections or requests in a given interval of time to be deemed as attack."
                type: integer
                format: int32
            min_value:
                description: "Minimum number of packets or connections or requests in a given interval of time to be deemed as attack."
                type: integer
                format: int32
    DosThresholdProfile:
        type: object
        required:
            -   thresh_period
        properties:
            thresh_info:
                type: array
                items:
                    $ref: '#/definitions/DosThreshold'
            thresh_period:
                description: "Timer value in seconds to collect DoS attack metrics based on threshold on the Service Engine for this Virtual Service."
                type: integer
                format: int32
    IpAddr:
        type: object
        required:
            -   addr
            -   type
        properties:
            addr:
                description: "IP address"
                type: string
            type:
                description: "Enum options - V4, DNS"
                type: number
                format: int64
    IpAddrPrefix:
        type: object
        required:
            -   ip_addr
            -   mask
        properties:
            ip_addr:
                $ref: '#/definitions/IpAddr'
            mask:
                description: ""
                type: integer
                format: int32
    IptableRule:
        type: object
        required:
            -   action
        properties:
            action:
                description: "Enum options - ACCEPT, DROP, REJECT, DNAT, MASQUERADE"
                type: number
                format: int64
            dnat_ip:
                $ref: '#/definitions/IpAddr'
            dst_ip:
                $ref: '#/definitions/IpAddrPrefix'
            dst_port:
                $ref: '#/definitions/PortRange'
            input_interface:
                description: ""
                type: string
            output_interface:
                description: ""
                type: string
            proto:
                description: "Enum options - PROTO_TCP, PROTO_UDP, PROTO_ICMP, PROTO_ALL"
                type: number
                format: int64
            src_ip:
                $ref: '#/definitions/IpAddrPrefix'
            src_port:
                $ref: '#/definitions/PortRange'
            tag:
                description: ""
                type: string
    IptableRuleSet:
        type: object
        required:
            -   chain
            -   table
        properties:
            chain:
                description: ""
                type: string
            rules:
                type: array
                items:
                    $ref: '#/definitions/IptableRule'
            table:
                description: ""
                type: string
    MetricsRealTimeUpdate:
        type: object
        required:
            -   enabled
        properties:
            duration:
                description: "Real time metrics collection duration in minutes. 0 for infinite."
                type: integer
                format: int32
            enabled:
                description: "Enables real time metrics collection.  When disabled, 6 hour view is the most granular the system will track."
                type: boolean
    PortRange:
        type: object
        required:
            -   end
            -   start
        properties:
            end:
                description: "TCP/UDP port range end (inclusive)."
                type: integer
                format: int32
            start:
                description: "TCP/UDP port range start (inclusive)."
                type: integer
                format: int32
    ServiceEngineGroup:
        type: object
        required:
            -   name
        properties:
            active_standby:
                description: "Service Engines in active/standby mode for HA failover"
                type: boolean
            advertise_backend_networks:
                description: "Advertise reach-ability of backend server networks via ADC through BGP for default gateway feature."
                type: boolean
            aggressive_failure_detection:
                description: "Enable aggressive failover configuration for ha."
                type: boolean
            algo:
                description: "If 'compact' placement algorithm is used, Virtual Services are placed on existing Service Engines until they all have the maximum number of Virtual Services. Otherwise, Virtual Services are distributed to as many Service Engines as possible.Enum options - PLACEMENT_ALGO_PACKED, PLACEMENT_ALGO_DISTRIBUTED"
                type: number
                format: int64
            auto_rebalance:
                description: "If 'Auto Rebalance' is selected, Virtual Services will be automatically migrated when the load on Service Engines falls below the minimum threshold or goes above the maximum threshold. Otherwise, an Alert is generated instead of automatically performing the migration."
                type: boolean
            auto_rebalance_interval:
                description: "Frequency of rebalance, if 'Auto rebalance' is enabled"
                type: integer
                format: int32
            auto_redistribute_active_standby_load:
                description: "Redistribution of virtual services from the takeover SE to the replacement SE can cause momentary traffic loss. If the auto-redistribute load option is left in its default off state, any desired rebalancing requires calls to REST API."
                type: boolean
            buffer_se:
                description: "Excess Service Engine capacity provisioned for HA failover"
                type: integer
                format: int32
            cloud_ref:
                description: " object ref Cloud."
                readOnly: True
                type: string
            connection_memory_percentage:
                description: "Percentage of memory for connection state. This will come at the expense of memory used for HTTP in-memory cache."
                type: integer
                format: int32
            cpu_reserve:
                description: ""
                type: boolean
            cpu_socket_affinity:
                description: "Allocate all the CPU cores for the Service Engine Virtual Machines  on the same CPU socket. Applicable only for vCenter Cloud."
                type: boolean
            custom_tag:
                type: array
                items:
                    $ref: '#/definitions/CustomTag'
            dedicated_dispatcher_core:
                description: "Dedicate the core that handles packet receive/transmit from the network to just the dispatching function. Don't use it for TCP/IP and SSL functions."
                type: boolean
            description:
                description: ""
                type: string
            disk_per_se:
                description: "Amount of disk space for each of the Service Engine virtual machines."
                type: integer
                format: int32
            distribute_load_active_standby:
                description: "Use both the active and standby Service Engines for Virtual Service placement in the legacy active standby HA mode."
                type: boolean
            enable_routing:
                description: "Enable routing for this ServiceEngineGroup "
                type: boolean
            extra_config_multiplier:
                description: "Multiplier for extra config to support large VS/Pool config."
                type: number
                format: double
            floating_intf_ip:
                type: array
                items:
                    $ref: '#/definitions/IpAddr'
            floating_intf_ip_se_2:
                type: array
                items:
                    $ref: '#/definitions/IpAddr'
            ha_mode:
                description: "High Availability mode for all the Virtual Services using this Service Engine group.Enum options - HA_MODE_SHARED_PAIR, HA_MODE_SHARED, HA_MODE_LEGACY_ACTIVE_STANDBY"
                type: number
                format: int64
            hardwaresecuritymodulegroup_ref:
                description: " object ref HardwareSecurityModuleGroup."
                type: string
            hm_on_standby:
                description: "Enable active health monitoring from the standby SE for all placed virtual services."
                type: boolean
            host_attribute_key:
                description: "Key of a Key,Value pair identifying a set of hosts. Currently used to separate North-South and East-West ServiceEngine sizing requirements, specifically in Container ecosystems, where ServiceEngines on East-West traffic nodes are typically smaller than those on North-South traffic nodes."
                type: string
            host_attribute_value:
                description: "Value of a Key,Value pair identifying a set of hosts. Currently used to separate North-South and East-West ServiceEngine sizing requirements, specifically in Container ecosystems, where ServiceEngines on East-West traffic nodes are typically smaller than those on North-South traffic nodes."
                type: string
            hypervisor:
                description: "Override default hypervisor. Enum options - DEFAULT, VMWARE_ESX, KVM, VMWARE_VSAN"
                type: number
                format: int64
            instance_flavor:
                description: "Instance/Flavor type for SE instance"
                type: string
            iptables:
                type: array
                items:
                    $ref: '#/definitions/IptableRuleSet'
            least_load_core_selection:
                description: "Select core with least load for new flow."
                type: boolean
            log_disksz:
                description: "Maximum disk capacity (in MB) to be allocated to an SE. This is exclusively used for debug and log data."
                type: integer
                format: int32
            max_cpu_usage:
                description: "When CPU utilization exceeds this maximum threshold, Virtual Services hosted on this Service Engine may be rebalanced to other Service Engines to lighten the load. A new Service Engine may be created as part of this process."
                type: integer
                format: int32
            max_scaleout_per_vs:
                description: "Maximum number of active Service Engines for the Virtual Service."
                type: integer
                format: int32
            max_se:
                description: "Maximum number of Services Engines in this group."
                type: integer
                format: int32
            max_vs_per_se:
                description: "Maximum number of Virtual Services that can be placed on a single Service Engine."
                type: integer
                format: int32
            mem_reserve:
                description: ""
                type: boolean
            memory_per_se:
                description: "Amount of memory for each of the Service Engine virtual machines."
                type: integer
                format: int32
            mgmt_network_ref:
                description: "Management network to use for Avi Service Engines object ref Network."
                type: string
            mgmt_subnet:
                $ref: '#/definitions/IpAddrPrefix'
            min_cpu_usage:
                description: "When CPU utilization falls below the minimum threshold, Virtual Services hosted on this Service Engine may be consolidated onto other underutilized Service Engines.  After consolidation, unused Service Engines may then be eligible for deletion. When CPU utilization exceeds the maximum threshold, Virtual Services hosted on this Service Engine may be migrated to other Service Engines to lighten the load. A new Service Engine may be created as part of this process."
                type: integer
                format: int32
            min_scaleout_per_vs:
                description: "Minimum number of active Service Engines for the Virtual Service."
                type: integer
                format: int32
            name:
                description: ""
                type: string
            num_flow_cores_sum_changes_to_ignore:
                description: "Number of changes in num flow cores sum to ignore."
                type: integer
                format: int32
            openstack_availability_zone:
                description: ""
                type: string
            openstack_mgmt_network_name:
                description: "Avi Management network name"
                type: string
            openstack_mgmt_network_uuid:
                description: "Management network UUID"
                type: string
            os_reserved_memory:
                description: "Amount of extra memory to be reserved for use by the Operating System on a Service Engine."
                type: integer
                format: int32
            per_app:
                description: "Per-app SE mode is designed for deploying dedicated load balancers per app (VS). In this mode, each SE is limited to a max of 2 VSs. vCPUs in per-app SEs count towards licensing usage at 25% rate."
                type: boolean
            placement_mode:
                description: "If placement mode is 'Auto', Virtual Services are automatically placed on Service Engines.Enum options - PLACEMENT_MODE_AUTO"
                type: number
                format: int64
            realtime_se_metrics:
                $ref: '#/definitions/MetricsRealTimeUpdate'
            se_deprovision_delay:
                description: "Duration to preserve unused Service Engine virtual machines before deleting them. If traffic to a Virtual Service were to spike up abruptly, this Service Engine would still be available to be utilized again rather than creating a new Service Engine. If this value is set to 0, Controller will never delete any Service Engines, administrator has to manually cleanup unused Service Engines."
                type: integer
                format: int32
            se_dos_profile:
                $ref: '#/definitions/DosThresholdProfile'
            se_name_prefix:
                description: "Prefix to use for virtual machine name of Service Engines."
                type: string
            tenant_ref:
                description: " object ref Tenant."
                type: string
            url:
                description: "url"
                readOnly: True
                type: string
            uuid:
                description: ""
                type: string
            vcenter_clusters:
                $ref: '#/definitions/VcenterClusters'
            vcenter_datastore_mode:
                description: "Enum options - VCENTER_DATASTORE_ANY, VCENTER_DATASTORE_LOCAL, VCENTER_DATASTORE_SHARED"
                type: number
                format: int64
            vcenter_datastores:
                type: array
                items:
                    $ref: '#/definitions/VcenterDatastore'
            vcenter_datastores_include:
                description: ""
                type: boolean
            vcenter_folder:
                description: "Folder to place all the Service Engine virtual machines in vCenter."
                type: string
            vcenter_hosts:
                $ref: '#/definitions/VcenterHosts'
            vcpus_per_se:
                description: "Number of vcpus for each of the Service Engine virtual machines."
                type: integer
                format: int32
            vs_host_redundancy:
                description: "Ensure primary and secondary Service Engines are deployed on different physical hosts."
                type: boolean
            vs_scalein_timeout:
                description: "Time to wait for the scaled in SE to drain existing flows before marking the scalein done"
                type: integer
                format: int32
            vs_scalein_timeout_for_upgrade:
                description: "During SE upgrade, Time to wait for the scaled-in SE to drain existing flows before marking the scalein done"
                type: integer
                format: int32
            vs_scaleout_timeout:
                description: "Time to wait for the scaled out SE to become ready before marking the scaleout done"
                type: integer
                format: int32
    ServiceEngineGroupApiResponse:
        type: object
        required:
            - count
            - results
        properties:
            count:
                type: integer
                format: int32
            results:
                type: array
                items:
                    $ref: "#/definitions/ServiceEngineGroup"
    VcenterClusters:
        type: object
        properties:
            cluster_refs:
                description: " object ref VIMgrClusterRuntime."
                type: array
                items:
                    type: string
            include:
                description: ""
                type: boolean
    VcenterDatastore:
        type: object
        required:
            -   datastore_name
        properties:
            datastore_name:
                description: ""
                type: string
    VcenterHosts:
        type: object
        properties:
            host_refs:
                description: " object ref VIMgrHostRuntime."
                type: array
                items:
                    type: string
            include:
                description: ""
                type: boolean
